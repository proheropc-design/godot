	// ==========================================
	// CUSTOM FRIENDLY UI PATCH FOR GODOT EDITOR
	// ==========================================
	if (theme.is_valid()) {
		theme->set_default_font_size(18 * p_scale);
	}
